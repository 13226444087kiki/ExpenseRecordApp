<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- 主活动 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 记录列表活动 -->
        <activity
            android:name=".ui.RecordListActivity"
            android:exported="false" />

        <!-- 广播接收器 -->
        <receiver
            android:name=".broadcast.ExpenseUpdateReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.expenserecordapp.action.EXPENSE_UPDATED" />
            </intent-filter>
        </receiver>

        <!-- 后台服务 -->
        <service
            android:name=".service.ExpenseMonitorService"
            android:enabled="true"
            android:exported="false" />

        <!-- ContentProvider -->
        <provider
            android:name=".ExpenseProvider"
            android:authorities="com.example.expenserecordapp.provider"
            android:exported="false"
            android:enabled="true" />

    </application>

</manifest>